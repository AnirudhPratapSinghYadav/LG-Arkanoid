# LG Arkanoid

A multiplayer brick breaker that spans all screens of a Liquid Galaxy rig as one continuous panoramic arcade wall.

**Repository:** [github.com/AnirudhPratapSinghYadav/LG-Arkanoid](https://github.com/AnirudhPratapSinghYadav/LG-Arkanoid)

## What is this

The idea is to take the classic Arkanoid/brick breaker experience and stretch it across the whole Liquid Galaxy panoramic wall instead of confining it to one screen. The physical screens act as one giant canvas - bricks span across all of them, and the ball can travel freely from Screen 1 all the way to the last screen without stuttering or disappearing when it crosses a bezel.

Up to three players join in using their phones (Android, built with Flutter) as paddle controllers. The phones don't render any game graphics themselves, they're purely input devices - everything actually happens on the 3 rig screens. When the ball crosses from one screen to the next, both screens update in the same frame so it looks seamless, running at 60fps.

There's also a bit of an AI gimmick on Screen 3 - live commentary generated by Gemini that reacts to what's happening in the game, kind of like a retro arcade announcer.

## Documentation Reference

The technical details of this project are documented extensively in the `docs/` directory:

- [**Architecture & Diagrams**](docs/architecture.md): The overall system design, boundary handoff protocol, Socket.IO flow, and Gemini commentary pipeline.
- [**Liquid Galaxy Setup**](docs/lg-setup.md): How to install, run, and configure the game on the master node (including the SSH launch pipeline).
- [**Mobile Controller Setup**](docs/mobile-setup.md): How to build and run the Flutter mobile app.
- [**Networking**](docs/networking.md): Ports, protocols, and firewall rules used by the game.
- [**Security**](docs/security.md): Security practices, rate limits, nonces, and API key management.
- [**Troubleshooting & Compatibility**](docs/troubleshooting.md): Resolving common rig issues, Node 14/16 ceiling constraints, and dependencies.

## Repo layout

```
LG-Arkanoid/
├── mobile/              Flutter phone controller app (docs/mobile-setup.md)
├── server/              Node.js backend, physics, and socket server
├── web-client/          Phaser 3 screen clients
├── scripts/             Liquid Galaxy integration scripts
├── docs/                Protocol and architecture documentation
└── README.md
```

## Running it

**Start the server**

```bash
cd server
npm install
node index.js
```

It listens on port 3000, you'll see `LG Arkanoid game server running on port 3000` in the console when it's up.

If you want live commentary working, set your Gemini key first:

```bash
export GEMINI_API_KEY=your_key_here
node index.js
```

**Get the rig connected and screens up**

The game uses an automated SSH connection flow to manage the Liquid Galaxy rig, avoiding manual browser windows.

1. Launch the mobile app and go to **Rig Connection**.
2. Enter the rig's Master Node IP, Port (default 22), Username (default `lg`), Password (encrypted securely on-device), and the total number of screens. Or simply tap **Scan QR** to auto-fill these details from a rig-generated QR code.
   > **Note:** For long-term production deployments, it is highly recommended to configure **SSH Keys** on the rig instead of relying on passwords. The launcher scripts now support passwordless SSH automatically if configured.
3. Tap **Connect to Rig**. The app automatically retries 5 times if the network is spotty.
4. Once connected, tap **Launch on Rig**. 
   - The app runs a script over SSH (`open-arkanoid.sh`) which automatically detects your rig topology (via `$LG_FRAMES`) and launches Chromium on all physical screens perfectly aligned.
   - Zero manual typing required on the rig.

Hit the "Start Match" button from the app's Lobby once your players are ready.

**Connect other players via Phone**

Other players simply launch the app and tap **Scan Liquid Galaxy QR** to join your active session instantly. The master node handles the physics loop every 16ms and pushes state to all clients.

## Local Network Testing

You can run the live reload Vite dev server on your development machine and instantly preview and control the game from any mobile device over the same Wi-Fi network.

### 1. Start the Dev Server
From the project root directory, run:
```bash
npm start
```
This single command runs both the Node.js backend server (`0.0.0.0:3000`) and the Vite live-reload dev server (`0.0.0.0:5173`) concurrently.

### 2. Find Your Local IP Address
Run this Node command to quickly find your local IPv4 address:
```bash
node -e "const os = require('os'); const nets = os.networkInterfaces(); for (const k in nets) for (const n of nets[k]) if (n.family === 'IPv4' && !n.internal) console.log(k, n.address);"
```
(Alternatively, use `ipconfig` on Windows, or `ifconfig`/`ip a` on macOS/Linux).

### 3. Open on Your Mobile Device
Make sure your mobile device is connected to the **same Wi-Fi network**.

- **Web Controller (Mobile):**
  Open any browser on your phone and navigate to:
  `http://<YOUR-LOCAL-IP>:5173` (e.g., `http://192.168.1.10:5173`)

- **Game Screen Preview:**
  Open a browser on your phone or desktop and navigate to:
  `http://<YOUR-LOCAL-IP>:5173/1`

- **Live Reload:**
  Every time you save a code change in `web-client/` (HTML/CSS/JS), all connected browsers automatically reload immediately!

### 4. Install as Web App (PWA)
To run the web controller full-screen like a native app:
- **iOS (Safari):** Tap the **Share** button, scroll down, and tap **Add to Home Screen**.
- **Android (Chrome):** Tap the three-dot menu icon, and tap **Add to Home screen**.

### 5. Firewall Troubleshooting
If your device cannot reach the server, ensure your firewall allows incoming connections on ports **5173** and **3000**. On Windows, you may need to allow the **Node.js JavaScript Runtime** through the Windows Defender Firewall.


## GeminiSoC 2026

This project's being built as part of **Gemini Summer of Code 2026**, under the **Liquid Galaxy** org.

## Contributors

| Role | Name | GitHub |
|---|---|---|
| Contributor | Anirudh Pratap Singh Yadav | [@AnirudhPratapSinghYadav](https://github.com/AnirudhPratapSinghYadav) |
| Mentor | Sidharth Mudgil | [@SidharthMudgil](https://github.com/SidharthMudgil) |

Repo's public so anyone can follow along, learn from it, or jump in and contribute if they want.
